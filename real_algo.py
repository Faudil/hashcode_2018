
from hashcode import *

def sort_rides(rides, car):
    for ride in rides:
        car.total_ride_lenght


def real_solve(vehicles, rides, max_step):
    for step in range(0, max_step):
        for vehicle in vehicles:
            if vehicle._avail:
                print()
